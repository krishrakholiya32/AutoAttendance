fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic[email]==2.10.4
pydantic-settings==2.7.0
pyjwt==2.10.1
pwdlib[argon2]==0.2.1
python-multipart==0.0.20
insightface==1.0.1
onnxruntime==1.27.0
opencv-python-headless==4.11.0.86
numpy==2.2.4

# insightface's own setup.py lists "opencv-python" (GUI build) as a dependency,
# so pip installs it alongside opencv-python-headless above even though only
# one cv2 native module can be active -- on a minimal server (no libGL.so.1)
# the GUI build fails to import. After `pip install -r requirements.txt`, run:
#   pip uninstall -y opencv-python && pip install --force-reinstall --no-cache-dir opencv-python-headless==4.11.0.86
# to leave only the headless build installed.
